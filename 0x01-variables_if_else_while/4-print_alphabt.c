#include <stdio.h>

/**
 * main -Entry Point
 * Description: A program that prints alphabets with exceptions
 * Return: 0
*/
int main(void)
{
	int(ch);
	for (ch = "a..z"; ch != 'q'; ch != 'e'; ch++)
	putchar(ch);
	putchar('\n');

	return (0);
}
