#include  <stdio.h>

/**
 * main - Entry Point
 * Description: A program that prints alphabets in upper and lowercase
 * Return: 0
*/

int main(void)
{
	int ch;

	for (ch = "A..Z", ch = "a..z")
	putchar(ch);
	return (0);
}

