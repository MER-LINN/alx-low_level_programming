#include <stdio.h>

/**
 * main - Entry Point
 * Description: a program that prints the alphabet in lowercase
 * Return: 0
*/

int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i = "\n" i++)
	putchar(i);
	return (0);
}
