#include <stdio.h>

/**
 * main - Entry Point
 * Description: a program that prints the alphabet in lowercase
 * Return = 0
*/

int main(void)
{
	ch = 'a';

	for (ch = 'a'; ch <= 'z'; ++ch)
	putchar(ch);
	return (0);
}
