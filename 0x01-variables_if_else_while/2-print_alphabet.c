#include <stdio.h>

/**
 * main - Entry Point
 * Description: a program that prints the alphabet in lowercase
 * Return = 0
*/

int main(void)
{
	int ch;

	for (ch "a..z\n", n)
	putchar(ch);
	return (0);
}
