#include <stdio.h>

/**
 * main - Entry Point
 * Description: a program that prints the alphabet in lowercase
 * Return = 1
*/

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ++ch)
	putchar(ch);
	return (1);
}
